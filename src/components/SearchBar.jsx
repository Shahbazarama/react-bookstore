import React from 'react'

class SearchBar extends React.Component {
  state = {
  }

  render() {
    return (
      <div>
      <input placeholder="this is the search"></input>
      </div>
    )
  }
}

export default SearchBar
